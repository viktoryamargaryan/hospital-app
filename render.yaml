services:
  - type: web
    name: hospital-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app 
    packages:
      - unixodbc
      - unixodbc-dev
